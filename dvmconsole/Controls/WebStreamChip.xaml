<UserControl x:Class="dvmconsole.Controls.WebStreamChip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="178" Height="90"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <UserControl.Effect>
        <DropShadowEffect ShadowDepth="1" BlurRadius="16" Color="#FF686767" Opacity="0.3" />
    </UserControl.Effect>
    <Border x:Name="ControlBorder" BorderBrush="LightGray" BorderThickness="1.5" CornerRadius="8" Background="#FF404040">
        <Grid Margin="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="24" />
                <RowDefinition Height="18" />
                <RowDefinition Height="36" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="NameText"
                       Text="{Binding DisplayName}"
                       FontSize="12"
                       FontWeight="Bold"
                       Foreground="White"
                       TextTrimming="CharacterEllipsis"
                       ToolTip="{Binding StreamUrl}" />

            <TextBlock x:Name="StatusTextBlock"
                       Grid.Row="1"
                       Text="{Binding StatusText}"
                       FontSize="9"
                       Foreground="Gold"
                       TextTrimming="CharacterEllipsis" />

            <Rectangle Grid.Row="2"
                       HorizontalAlignment="Stretch"
                       Height="28"
                       VerticalAlignment="Center"
                       StrokeThickness="0"
                       RadiusX="4"
                       RadiusY="4"
                       PreviewMouseWheel="VolumeBox_PreviewMouseWheel">
                <Rectangle.Fill>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FFF0F0F0" Offset="0.485"/>
                        <GradientStop Color="Gainsboro" Offset="0.517"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Slider x:Name="VolumeSlider"
                    Grid.Row="2"
                    Minimum="0"
                    Maximum="4"
                    Value="{Binding Volume, Mode=TwoWay}"
                    SmallChange="0.1"
                    LargeChange="0.5"
                    TickFrequency="0.1"
                    IsSnapToTickEnabled="True"
                    IsMoveToPointEnabled="True"
                    VerticalAlignment="Center"
                    Height="22"
                    Margin="8,0,8,0"
                    ValueChanged="VolumeSlider_ValueChanged"
                    PreviewMouseWheel="VolumeSlider_PreviewMouseWheel"
                    Loaded="VolumeSlider_Loaded"
                    SizeChanged="VolumeSlider_SizeChanged"
                    ToolTip="Stream Volume">
                <Slider.Style>
                    <Style TargetType="Slider">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Slider">
                                    <Grid>
                                        <Border x:Name="PART_DefaultVolumeMarker"
                                                Width="2"
                                                Height="16"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Background="#AA303030"
                                                IsHitTestVisible="False"
                                                Panel.ZIndex="0" />
                                        <Rectangle Name="PART_TrackBackground"
                                                   Height="4"
                                                   Fill="Gray"
                                                   VerticalAlignment="Center"
                                                   Margin="4"
                                                   Panel.ZIndex="1"/>
                                        <Track Name="PART_Track" VerticalAlignment="Center" Panel.ZIndex="2">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Width="0" Background="Transparent" BorderThickness="0"/>
                                            </Track.DecreaseRepeatButton>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Width="0" Background="Transparent" BorderThickness="0"/>
                                            </Track.IncreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb Name="PART_Thumb" Width="10" Height="20" Background="DarkGray"/>
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Slider.Style>
            </Slider>
        </Grid>
    </Border>
</UserControl>
