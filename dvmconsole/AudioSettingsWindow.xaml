<Window x:Class="dvmconsole.AudioSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}" TextElement.FontWeight="Regular" TextElement.FontSize="13"
        Style="{StaticResource MaterialDesignWindow}" Background="{DynamicResource MaterialDesignPaper}" FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Audio Settings" Height="560" MinHeight="460" Width="760" MinWidth="640" Icon="/dvmconsole;component/Assets/audio.png"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="AudioTabNavButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonChrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonChrome" Property="Background" Value="{DynamicResource MaterialDesignDivider}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonChrome" Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Global Input Device" FontWeight="Bold" Margin="0,0,0,4"/>
                <ComboBox x:Name="InputDeviceComboBox"
                          SelectedValuePath="DeviceKey"
                          DisplayMemberPath="DisplayName"/>
                <TextBlock Text="Use Windows Default unless this console should always use a specific mic."
                           Opacity="0.72" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Master Output Device" FontWeight="Bold" Margin="0,0,0,4"/>
                <ComboBox x:Name="MasterOutputComboBox"
                          SelectedValuePath="DeviceKey"
                          DisplayMemberPath="DisplayName"/>
                <TextBlock Text="Resources use this output unless an override is selected below."
                           Opacity="0.72" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <Border Background="{DynamicResource MaterialDesignDivider}" Height="1" Margin="0,0,0,10"/>
            <CheckBox x:Name="AgcToggle"
                      Content="Enable AGC for console microphone audio"
                      ToolTip="Automatic gain control adjusts console mic level before transmit. Leave off for the current raw microphone behavior."/>
        </StackPanel>

        <TabControl x:Name="ZoneRoutingTabs"
                    Grid.Row="2"
                    Margin="0,0,0,14"
                    Background="{DynamicResource MaterialDesignPaper}"
                    Foreground="{DynamicResource MaterialDesignBody}">
            <TabControl.Style>
                <Style TargetType="{x:Type TabControl}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabControl}">
                                <Grid KeyboardNavigation.TabNavigation="Local">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0"
                                          Background="{DynamicResource MaterialDesignPaper}"
                                          Margin="0,0,0,-1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Button x:Name="ScrollTabsLeftButton"
                                                Grid.Column="0"
                                                Width="28"
                                                Height="26"
                                                Padding="0"
                                                Margin="0,0,4,2"
                                                Style="{StaticResource AudioTabNavButtonStyle}"
                                                ToolTip="Scroll tabs left">
                                            <TextBlock Text="&#x2039;"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                        </Button>

                                        <ScrollViewer x:Name="TabHeaderScrollViewer"
                                                      Grid.Column="1"
                                                      HorizontalScrollBarVisibility="Hidden"
                                                      VerticalScrollBarVisibility="Disabled"
                                                      CanContentScroll="False"
                                                      Focusable="False"
                                                      Background="{DynamicResource MaterialDesignPaper}">
                                            <TabPanel x:Name="HeaderPanel"
                                                      IsItemsHost="True"
                                                      KeyboardNavigation.TabIndex="1"
                                                      Panel.ZIndex="1"/>
                                        </ScrollViewer>

                                        <Button x:Name="ScrollTabsRightButton"
                                                Grid.Column="2"
                                                Width="28"
                                                Height="26"
                                                Padding="0"
                                                Margin="4,0,0,2"
                                                Style="{StaticResource AudioTabNavButtonStyle}"
                                                ToolTip="Scroll tabs right">
                                            <TextBlock Text="&#x203A;"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                        </Button>
                                    </Grid>

                                    <Border Grid.Row="1"
                                            BorderBrush="{DynamicResource MaterialDesignDivider}"
                                            BorderThickness="1"
                                            Background="{DynamicResource MaterialDesignPaper}">
                                        <ContentPresenter x:Name="PART_SelectedContentHost"
                                                          ContentSource="SelectedContent"
                                                          Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Style>
        </TabControl>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3">
            <Button x:Name="SaveButton" Content="Save" Width="100" Margin="0,0,10,0" Click="SaveButton_Click"
                    IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Style="{StaticResource MaterialDesignPaperDarkButton}" />
            <Button x:Name="CancelButton" Content="Cancel" Width="100" Click="CancelButton_Click"
                    IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Style="{StaticResource MaterialDesignPaperDarkButton}" />
        </StackPanel>
    </Grid>
</Window>
