<Window x:Class="dvmconsole.TarConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="TAR Configuration"
        Width="980"
        Height="560"
        MinWidth="840"
        MinHeight="460"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Icon="/dvmconsole;component/Assets/AppIcon.ico">
    <Window.Resources>
        <Style x:Key="TarTabNavButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonChrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonChrome" Property="Background" Value="{DynamicResource MaterialDesignDivider}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonChrome" Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="18"
                       FontWeight="SemiBold"
                       Text="Talkgroup Audio Recorder" />
            <TextBlock Margin="0,6,0,0"
                       Opacity="0.8"
                       Text="Choose where TAR saves recordings, then enable recording per talkgroup and set retention or ignored subscriber IDs." />
        </StackPanel>

        <Border Grid.Row="2"
                Padding="16"
                CornerRadius="8"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="1"
                Background="{DynamicResource MaterialDesignCardBackground}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           FontWeight="SemiBold"
                           Text="Recording Folder" />

                <DockPanel Grid.Row="2"
                           Grid.ColumnSpan="2"
                           LastChildFill="True">
                    <Button Content="Browse"
                            Width="90"
                            Height="34"
                            Margin="12,0,0,0"
                            DockPanel.Dock="Right"
                            Click="BrowseRecordingFolder_Click"
                            Style="{StaticResource MaterialDesignOutlinedButton}" />
                    <TextBox Text="{Binding RecordingFolderPath, UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center"
                             materialDesign:HintAssist.Hint="Select a TAR recordings folder" />
                </DockPanel>
            </Grid>
        </Border>

        <TabControl x:Name="ZoneTabs"
                    Grid.Row="4"
                    ItemsSource="{Binding ZoneGroups}"
                    Background="{DynamicResource MaterialDesignPaper}"
                    Foreground="{DynamicResource MaterialDesignBody}">
            <TabControl.Style>
                <Style TargetType="{x:Type TabControl}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabControl}">
                                <Grid KeyboardNavigation.TabNavigation="Local">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0"
                                          Background="{DynamicResource MaterialDesignPaper}"
                                          Margin="0,0,0,-1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Button x:Name="ScrollTabsLeftButton"
                                                Grid.Column="0"
                                                Width="28"
                                                Height="26"
                                                Padding="0"
                                                Margin="0,0,4,2"
                                                Style="{StaticResource TarTabNavButtonStyle}"
                                                ToolTip="Scroll tabs left">
                                            <TextBlock Text="&#x2039;"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                        </Button>

                                        <ScrollViewer x:Name="TabHeaderScrollViewer"
                                                      Grid.Column="1"
                                                      HorizontalScrollBarVisibility="Hidden"
                                                      VerticalScrollBarVisibility="Disabled"
                                                      CanContentScroll="False"
                                                      Focusable="False"
                                                      Background="{DynamicResource MaterialDesignPaper}">
                                            <TabPanel x:Name="HeaderPanel"
                                                      IsItemsHost="True"
                                                      KeyboardNavigation.TabIndex="1"
                                                      Panel.ZIndex="1"/>
                                        </ScrollViewer>

                                        <Button x:Name="ScrollTabsRightButton"
                                                Grid.Column="2"
                                                Width="28"
                                                Height="26"
                                                Padding="0"
                                                Margin="4,0,0,2"
                                                Style="{StaticResource TarTabNavButtonStyle}"
                                                ToolTip="Scroll tabs right">
                                            <TextBlock Text="&#x203A;"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                        </Button>
                                    </Grid>

                                    <Border Grid.Row="1"
                                            BorderBrush="{DynamicResource MaterialDesignDivider}"
                                            BorderThickness="1"
                                            Background="{DynamicResource MaterialDesignCardBackground}">
                                        <ContentPresenter x:Name="PART_SelectedContentHost"
                                                          ContentSource="SelectedContent"
                                                          Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Style>
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ZoneName}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="8" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Opacity="0.72"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   Text="Enable TAR on the talkgroups in this tab. Retention and ignored subscriber IDs are stored by TGID." />

                        <DataGrid Grid.Row="2"
                                  ItemsSource="{Binding Channels}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  HeadersVisibility="Column"
                                  IsReadOnly="False"
                                  Margin="0"
                                  GridLinesVisibility="Horizontal"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Record"
                                                        Width="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="System"
                                                    Binding="{Binding SystemName}"
                                                    Width="180"
                                                    IsReadOnly="True" />
                                <DataGridTextColumn Header="Channel"
                                                    Binding="{Binding ChannelName}"
                                                    Width="220"
                                                    IsReadOnly="True" />
                                <DataGridTextColumn Header="TG"
                                                    Binding="{Binding TalkgroupId}"
                                                    Width="100"
                                                    IsReadOnly="True" />
                                <DataGridTextColumn Header="Mode"
                                                    Binding="{Binding Mode}"
                                                    Width="90"
                                                    IsReadOnly="True" />
                                <DataGridTextColumn Header="Keep Days"
                                                    Binding="{Binding RetentionDays, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    Width="95" />
                                <DataGridTextColumn Header="Ignore RIDs"
                                                    Binding="{Binding IgnoredSubscriberIdsText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <TextBlock Grid.Row="6"
                   x:Name="StatusTextBlock"
                   Foreground="#FF2E7D32"
                   Visibility="Collapsed" />

        <DockPanel Grid.Row="8"
                   LastChildFill="False">
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Left">
                <Button Content="Close"
                        Width="90"
                        Height="34"
                        Click="Close_Click"
                        Style="{StaticResource MaterialDesignOutlinedButton}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right">
                <Button x:Name="SaveButton"
                        Content="Save"
                        Width="90"
                        Height="34"
                        Click="Save_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
