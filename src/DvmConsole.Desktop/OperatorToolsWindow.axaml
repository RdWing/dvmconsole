<!-- SPDX-FileCopyrightText: 2025-2026 RdWing -->
<!-- SPDX-License-Identifier: AGPL-3.0-only -->

<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:DvmConsole.Desktop"
        x:Class="DvmConsole.Desktop.OperatorToolsWindow"
        x:DataType="local:MainWindowViewModel"
        Title="Console Settings" Width="1180" Height="780" MinWidth="720" MinHeight="520" Background="{DynamicResource ShellBackgroundBrush}" FontSize="{Binding UiFontSize}"
        WindowStartupLocation="CenterScreen">
  <Window.Styles>
    <Style Selector="TextBlock.heading"><Setter Property="FontSize" Value="{Binding $parent[Window].((local:MainWindowViewModel)DataContext).UiHeadingFontSize}" /><Setter Property="FontWeight" Value="SemiBold" /></Style>
    <Style Selector="TextBlock.caption"><Setter Property="Opacity" Value="0.72" /><Setter Property="FontSize" Value="{Binding $parent[Window].((local:MainWindowViewModel)DataContext).UiSmallFontSize}" /></Style>
    <Style Selector="Border.panel"><Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" /><Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" /><Setter Property="BorderThickness" Value="1" /><Setter Property="CornerRadius" Value="6" /><Setter Property="Padding" Value="10" /><Setter Property="Margin" Value="0" /></Style>
    <Style Selector="Button"><Setter Property="Padding" Value="10,6" /></Style>
    <Style Selector="Border.history-row"><Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" /><Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" /><Setter Property="BorderThickness" Value="1" /><Setter Property="CornerRadius" Value="4" /><Setter Property="Padding" Value="6,4" /><Setter Property="Margin" Value="0,0,0,3" /></Style>
    <Style Selector="Button.history-action"><Setter Property="Padding" Value="7,3" /><Setter Property="MinHeight" Value="0" /></Style>
    <Style Selector="ListBox.history-list > ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch" /><Setter Property="Padding" Value="0" /><Setter Property="Margin" Value="0" /><Setter Property="MinHeight" Value="0" /></Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch" /><Setter Property="Padding" Value="9,7" /><Setter Property="Margin" Value="0,1" /><Setter Property="CornerRadius" Value="4" /></Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected"><Setter Property="Background" Value="#244E73" /><Setter Property="Foreground" Value="White" /><Setter Property="FontWeight" Value="SemiBold" /></Style>
    <Style Selector="ScrollViewer"><Setter Property="AllowAutoHide" Value="True" /><Setter Property="VerticalScrollBarVisibility" Value="Hidden" /></Style>
    <Style Selector="ScrollBar"><Setter Property="AllowAutoHide" Value="True" /><Setter Property="ShowDelay" Value="0:0:0" /><Setter Property="HideDelay" Value="0:0:0.7" /></Style>
  </Window.Styles>

  <LayoutTransformControl LayoutTransform="{Binding UiScaleTransform}">
  <Grid RowDefinitions="*,Auto,Auto">
    <Grid ColumnDefinitions="220,*">
      <Border Background="{DynamicResource ShellHeaderBrush}"
              BorderBrush="{DynamicResource ControlBorderBrush}"
              BorderThickness="0,0,1,0"
              Padding="10">
        <Grid RowDefinitions="Auto,Auto,*,Auto" RowSpacing="8">
          <StackPanel Spacing="2">
            <TextBlock FontWeight="SemiBold" Text="Console Settings" />
            <TextBlock Classes="caption" Text="Find a setting or operator tool" />
          </StackPanel>
          <TextBox x:Name="SectionSearchBox"
                   Grid.Row="1"
                   Watermark="Search settings"
                   AutomationProperties.Name="Search console settings"
                   TextChanged="HandleSectionSearchTextChanged" />
          <ListBox x:Name="SectionNavigation"
                   Grid.Row="2"
                   Classes="settings-navigation"
                   Background="Transparent"
                   BorderThickness="0"
                   SelectionChanged="HandleSectionNavigationChanged" />
          <TextBlock x:Name="NoSettingsSearchResults"
                     Grid.Row="3"
                     Classes="caption"
                     Text="No matching sections"
                     IsVisible="False" />
        </Grid>
      </Border>

      <ContentControl x:Name="ToolContent"
                      Grid.Column="1"
                      HorizontalContentAlignment="Stretch"
                      VerticalContentAlignment="Stretch" />
    </Grid>
    <Border Grid.Row="1"
            IsVisible="{Binding HasUndoNotification}"
            Background="{DynamicResource WarningBackgroundBrush}"
            BorderBrush="{DynamicResource WarningBorderBrush}"
            BorderThickness="0,1,0,0"
            Padding="10,6">
      <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
        <TextBlock Text="{Binding UndoNotificationText}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
        <Button Grid.Column="1" Content="Undo" AutomationProperties.Name="Undo last settings action" Click="HandleUndoLastOperatorActionClick" />
      </Grid>
    </Border>
    <Border Grid.Row="2" BorderBrush="#3A4654" BorderThickness="0,1,0,0" Padding="10"><Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="5"><TextBlock Classes="caption" FontWeight="SemiBold" Text="Global audio status:" VerticalAlignment="Center" /><TextBlock Grid.Column="1" Classes="caption" Text="{Binding AudioStatusText}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" /><Button Grid.Column="2" Content="Close" Click="HandleCloseClick" /></Grid></Border>
  </Grid>
  </LayoutTransformControl>
</Window>
