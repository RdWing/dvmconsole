<!-- SPDX-FileCopyrightText: 2025-2026 RdWing -->
<!-- SPDX-License-Identifier: AGPL-3.0-only -->

<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:presentation="using:DvmConsole.Presentation"
             x:Class="DvmConsole.Presentation.CallHistoryView"
             x:DataType="presentation:ICallHistoryViewModel">
  <UserControl.Styles>
    <Style Selector="StackPanel.filter-field"><Setter Property="Orientation" Value="Horizontal"/><Setter Property="Spacing" Value="6"/></Style>
    <Style Selector="StackPanel.filter-text-field"><Setter Property="Spacing" Value="6"/></Style>
    <Style Selector="TextBlock.filter-label"><Setter Property="IsVisible" Value="False"/></Style>
    <Style Selector="WrapPanel.filter-group"><Setter Property="ItemSpacing" Value="8"/><Setter Property="LineSpacing" Value="8"/></Style>
    <Style Selector="presentation|CallHistoryView.touch-history StackPanel.filter-field">
      <Setter Property="Orientation" Value="Vertical"/><Setter Property="Spacing" Value="8"/>
    </Style>
    <Style Selector="presentation|CallHistoryView.touch-history TextBlock.filter-label"><Setter Property="IsVisible" Value="True"/></Style>
    <Style Selector="presentation|CallHistoryView.touch-history WrapPanel.filter-group">
      <Setter Property="ItemSpacing" Value="16"/><Setter Property="LineSpacing" Value="16"/><Setter Property="Margin" Value="0,8,0,12"/>
    </Style>
    <Style Selector="presentation|CallHistoryView.touch-history Expander TextBox, presentation|CallHistoryView.touch-history Expander DatePicker">
      <Setter Property="MinHeight" Value="44"/>
    </Style>

    <Style Selector="Border.history-row">
      <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="Padding" Value="6,4" />
      <Setter Property="Margin" Value="0,0,0,3" />
    </Style>
    <Style Selector="Button.history-action">
      <Setter Property="Padding" Value="7,3" />
      <Setter Property="MinHeight" Value="0" />
    </Style>
    <Style Selector="WrapPanel.history-actions">
      <Setter Property="Grid.Column" Value="2" />
      <Setter Property="Grid.RowSpan" Value="3" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="HorizontalAlignment" Value="Right" />
    </Style>
    <Style Selector="WrapPanel.history-narrow-details">
      <Setter Property="IsVisible" Value="False" />
    </Style>
    <Style Selector="ListBox.history-list > ListBoxItem">
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Margin" Value="0" />
      <Setter Property="MinHeight" Value="0" />
    </Style>
    <Style Selector="TextBlock.history-heading">
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style Selector="TextBlock.history-caption">
      <Setter Property="Opacity" Value="0.72" />
    </Style>
    <Style Selector="Expander /template/ ToggleButton#PART_Header">
      <Setter Property="AutomationProperties.Name" Value="Advanced history filters" />
    </Style>
    <Style Selector="presentation|ResponsiveCallHistoryRow:narrow Grid.history-wide-details">
      <Setter Property="IsVisible" Value="False" />
    </Style>
    <Style Selector="presentation|ResponsiveCallHistoryRow:narrow WrapPanel.history-narrow-details">
      <Setter Property="IsVisible" Value="True" />
    </Style>
    <Style Selector="presentation|ResponsiveCallHistoryRow:narrow WrapPanel.history-actions">
      <Setter Property="Grid.Row" Value="3" />
      <Setter Property="Grid.Column" Value="0" />
      <Setter Property="Grid.RowSpan" Value="1" />
      <Setter Property="Grid.ColumnSpan" Value="2" />
      <Setter Property="HorizontalAlignment" Value="Left" />
      <Setter Property="Margin" Value="0,3,0,0" />
    </Style>
    <Style Selector="presentation|CallHistoryView.touch-history Button">
      <Setter Property="MinHeight" Value="44" />
    </Style>
    <Style Selector="presentation|CallHistoryView.touch-history TextBox">
      <Setter Property="MinHeight" Value="44" />
    </Style>
    <Style Selector="presentation|CallHistoryView.touch-history ComboBox">
      <Setter Property="MinHeight" Value="44" />
    </Style>
    <Style Selector="presentation|CallHistoryView.touch-history DatePicker">
      <Setter Property="MinHeight" Value="44" />
    </Style>
  </UserControl.Styles>

  <Grid x:Name="HistoryLayout" Margin="10" RowDefinitions="Auto,Auto,*" RowSpacing="10">
    <Grid x:Name="HistoryHeader" RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto" RowSpacing="8" ColumnSpacing="8">
      <TextBlock Classes="history-heading" Text="Event History" VerticalAlignment="Center" />
      <WrapPanel x:Name="HistoryHeaderActions" Grid.Column="1" ItemSpacing="8">
        <Button x:Name="ExportHistoryButton" Content="Export CSV…" AutomationProperties.Name="Export call history as CSV" Click="HandleExportClick" />
        <Button x:Name="ClearHistoryButton" Content="Clear session" AutomationProperties.Name="Clear session history" Click="HandleClearClick" />
      </WrapPanel>
      <TextBox x:Name="HistorySearch" Grid.Row="1" Grid.ColumnSpan="2"
               Watermark="Search events, calls, aliases, IDs, filenames, and diagnostics"
               Text="{Binding CallHistoryFilterText, Mode=TwoWay}" />
    </Grid>
    <Expander x:Name="HistoryFilters" Grid.Row="1" Header="Advanced filters">
      <ScrollViewer x:Name="HistoryFilterScroller" HorizontalScrollBarVisibility="Disabled">
      <StackPanel Spacing="8" Margin="0,8,0,0">
        <WrapPanel Classes="filter-group">
          <StackPanel Classes="filter-field"><TextBlock Text="Direction" VerticalAlignment="Center" /><ComboBox AutomationProperties.Name="Direction filter" Width="140" ItemsSource="{Binding RecordingDirectionFilters}" SelectedItem="{Binding RecordingDirectionFilter, Mode=TwoWay}" /></StackPanel>
          <StackPanel Classes="filter-field"><TextBlock Text="Protocol" VerticalAlignment="Center" /><ComboBox AutomationProperties.Name="Protocol filter" Width="140" ItemsSource="{Binding RecordingProtocolFilters}" SelectedItem="{Binding RecordingProtocolFilter, Mode=TwoWay}" /></StackPanel>
          <StackPanel Classes="filter-field"><TextBlock Text="Encryption" VerticalAlignment="Center" /><ComboBox AutomationProperties.Name="Encryption filter" Width="140" ItemsSource="{Binding RecordingEncryptionFilters}" SelectedItem="{Binding RecordingEncryptionFilter, Mode=TwoWay}" /></StackPanel>
          <Button Content="Clear filters" AutomationProperties.Name="Clear call history filters" Click="HandleClearFiltersClick" />
        </WrapPanel>
        <WrapPanel Classes="filter-group">
          <StackPanel Classes="filter-text-field" Width="150"><TextBlock Classes="filter-label" Text="System"/><TextBox Watermark="System" AutomationProperties.Name="System filter" Text="{Binding RecordingSystemFilterText, Mode=TwoWay}" /></StackPanel>
          <StackPanel Classes="filter-text-field" Width="150"><TextBlock Classes="filter-label" Text="Channel"/><TextBox Watermark="Channel" AutomationProperties.Name="Channel filter" Text="{Binding RecordingChannelFilterText, Mode=TwoWay}" /></StackPanel>
          <StackPanel Classes="filter-text-field" Width="150"><TextBlock Classes="filter-label" Text="Talkgroup ID"/><TextBox Watermark="Talkgroup ID" AutomationProperties.Name="Talkgroup ID filter" Text="{Binding RecordingTalkgroupFilterText, Mode=TwoWay}" /></StackPanel>
          <StackPanel Classes="filter-text-field" Width="150"><TextBlock Classes="filter-label" Text="Subscriber ID"/><TextBox Watermark="Subscriber ID" AutomationProperties.Name="Subscriber ID filter" Text="{Binding RecordingSubscriberFilterText, Mode=TwoWay}" /></StackPanel>
          <StackPanel Classes="filter-text-field" Width="150"><TextBlock Classes="filter-label" Text="Alias"/><TextBox Watermark="Alias" AutomationProperties.Name="Alias filter" Text="{Binding RecordingAliasFilterText, Mode=TwoWay}" /></StackPanel>
        </WrapPanel>
        <WrapPanel Classes="filter-group">
          <StackPanel Classes="filter-field"><TextBlock Text="From" VerticalAlignment="Center" /><DatePicker Width="190" SelectedDate="{Binding RecordingStartDateFilter, Mode=TwoWay}" /></StackPanel>
          <StackPanel Classes="filter-field"><TextBlock Text="Through" VerticalAlignment="Center" /><DatePicker Width="190" SelectedDate="{Binding RecordingEndDateFilter, Mode=TwoWay}" /></StackPanel>
          <TextBlock Classes="history-caption" Text="{Binding HistoryFilterSummary}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
        </WrapPanel>
      </StackPanel>
      </ScrollViewer>
    </Expander>
    <ListBox x:Name="HistoryList"
             Grid.Row="2"
             Classes="history-list"
             ItemsSource="{Binding FilteredCallHistory}"
             Background="Transparent"
             BorderThickness="0"
             AutoScrollToSelectedItem="False"
             ScrollViewer.BringIntoViewOnFocusChange="False">
      <ListBox.ItemsPanel>
        <ItemsPanelTemplate><VirtualizingStackPanel /></ItemsPanelTemplate>
      </ListBox.ItemsPanel>
      <ListBox.ItemTemplate>
        <DataTemplate x:DataType="presentation:ICallHistoryItemViewModel">
          <Border Classes="history-row">
            <presentation:ResponsiveCallHistoryRow Classes="history-row-layout">
              <TextBlock Text="{Binding TimestampText}" VerticalAlignment="Center" />
              <TextBlock Grid.Column="1" FontWeight="SemiBold" Text="{Binding DisplayChannelText}" TextTrimming="CharacterEllipsis" />
              <Grid Grid.Row="1"
                    Grid.ColumnSpan="2"
                    Classes="history-wide-details"
                    ColumnDefinitions="2*,Auto,Auto,Auto,Auto,*"
                    ColumnSpacing="14">
                <TextBlock Classes="history-caption" Text="{Binding SystemName}" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="1" Classes="history-caption" Text="{Binding DirectionText}" />
                <TextBlock Grid.Column="2" Classes="history-caption" Text="{Binding ProtocolText}" />
                <TextBlock Grid.Column="3" Classes="history-caption history-duration" Text="{Binding DurationText}" />
                <TextBlock Grid.Column="4"
                           Classes="history-caption history-encryption"
                           Text="{Binding EncryptionText}"
                           TextTrimming="CharacterEllipsis" />
              </Grid>
              <WrapPanel Grid.Row="1"
                         Grid.ColumnSpan="2"
                         Classes="history-narrow-details"
                         ItemSpacing="8"
                         LineSpacing="4">
                <TextBlock Classes="history-caption" Text="{Binding SystemName}" TextTrimming="CharacterEllipsis" />
                <TextBlock Classes="history-caption" Text="{Binding DirectionText}" />
                <TextBlock Classes="history-caption" Text="{Binding ProtocolText}" />
                <TextBlock Classes="history-caption" Text="{Binding DurationText}" />
                <TextBlock Classes="history-caption" Text="{Binding EncryptionText}" TextTrimming="CharacterEllipsis" />
              </WrapPanel>
              <TextBlock Grid.Row="2" Classes="history-caption" Text="{Binding DateText}" />
              <TextBlock Grid.Row="2" Grid.Column="1" Classes="history-caption" Text="{Binding RouteText}" TextTrimming="CharacterEllipsis" />
              <WrapPanel Classes="history-actions"
                         ItemSpacing="4"
                         IsVisible="{Binding HasRecording}">
                <Button Classes="history-action"
                        Content="{Binding RecordingPlaybackActionText}"
                        AutomationProperties.Name="{Binding RecordingPlaybackActionText}"
                        AutomationProperties.HelpText="{Binding RecordingPlaybackHelpText}"
                        Tag="{Binding}"
                        IsEnabled="{Binding HasPlayableRecording}"
                        ToolTip.Tip="{Binding RecordingPlaybackHelpText}"
                        Click="HandlePlaybackToggleClick" />
                <Button Classes="history-action" Content="{Binding $parent[presentation:CallHistoryView].RecordingOpenActionText}" AutomationProperties.Name="{Binding $parent[presentation:CallHistoryView].RecordingOpenActionText}" Tag="{Binding}" IsEnabled="{Binding HasPlayableRecording}" ToolTip.Tip="{Binding RecordingDetailsText}" Click="HandleOpenClick" />
                <Button Classes="history-action" Content="Delete" AutomationProperties.Name="Delete call recording" Tag="{Binding}" Click="HandleDeleteClick" />
              </WrapPanel>
            </presentation:ResponsiveCallHistoryRow>
          </Border>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</UserControl>
